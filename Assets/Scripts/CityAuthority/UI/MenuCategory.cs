namespace CityAuthority.UI
{
    // The left-side menu's fixed category set — one submenu per system,
    // mirroring EmergencyDebugPanel's existing sections 1:1.
    public enum MenuCategory
    {
        Emergency,
        SaveLoad,
        Development,
        Court,
        Media,
        Reports
    }
}
